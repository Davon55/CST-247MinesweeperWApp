
@model MinesweeperApp.Models.CellModel
@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
    <div>
        <h1>Welcome to Minesweeper</h1>
        @using (Html.BeginForm("HandleButtonClick", "BoardGame"))
        {
            for (int i = 0; i < Model.Size; i++)
            {
                <button type="submit" name="mine" value=@i>
                    <img src="~/img/minesweeper.jpg" />
                </button>
            }
            for (int i = 0; i < Model.Size; i++)
            {
                for (int j = 0; j < Model.Size; j++)
                {
                    if (Model.isVisited == true)
                    {

                    }
                }
            }
        }



    </div>
</body>
</html>
